{
  "name": "lambda-ts-template",
  "version": "1.0.0",
  "description": "A template for creating AWS Lambda functions using TypeScript and SAM",
  "author": "Bruno Petrolini <brunopetrolini@hotmail.com>",
  "license": "MIT",
  "scripts": {
    "lint": "biome check --write .",
    "format": "biome format --write .",
    "style:fix": "npm run lint && npm run format",
    "compile": "tsc",
    "test": "vitest run",
    "test:watch": "vitest --watch",
    "test:coverage": "vitest run --coverage"
  },
  "dependencies": {
    "esbuild": "^0.14.14",
    "zod": "3.23.8"
  },
  "devDependencies": {
    "@biomejs/biome": "1.9.4",
    "@types/aws-lambda": "^8.10.92",
    "@types/node": "^20.5.7",
    "@vitest/coverage-v8": "2.1.2",
    "typescript": "^4.8.4",
    "vitest": "^2.1.2",
    "vitest-tsconfig-paths": "^3.4.1"
  }
}
